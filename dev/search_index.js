var documenterSearchIndex = {"docs":
[{"category":"section","location":"tutorial_iris/#Tutorial-1:-Tabular-Data-Iris-Dataset","page":"Iris Dataset","text":"In this tutorial, we demonstrate how to use Trimap.jl to reduce the 4-dimensional Fisher's Iris dataset to 2 dimensions, comparing PCA initialization with random noise initialization, and visualizing the clusters interactively using PlotlyLight.jl.\n\n","title":"Tutorial 1: Tabular Data - Iris Dataset"},{"category":"section","location":"tutorial_iris/#1.-Loading-the-Iris-Dataset","page":"Iris Dataset","text":"We load the standard 150 samples across 3 classes (setosa, versicolor, virginica), each with 4 attributes (sepal length, sepal width, petal length, petal width).\n\nusing SimilaritySearch, Trimap\nusing MLDatasets, DataFrames\nusing PlotlyLight, Random, Statistics\n\n# Load Fisher Iris Dataset from MLDatasets.jl\ndataset = Iris()\n\n# Features matrix: (4 × 150) Float32 matrix\nX = Matrix{Float32}(transpose(Matrix(dataset.features)))\nlabels = String.(dataset.targets.class)\nunique_species = unique(labels)\n\nprintln(\"Dataset size: \", size(X))\nprintln(\"Classes: \", unique_species)\n\n","title":"1. Loading the Iris Dataset"},{"category":"section","location":"tutorial_iris/#2.-Baseline:-2D-PCA-Projection","page":"Iris Dataset","text":"Before applying non-linear dimensionality reduction, we compute a standard linear 2D Principal Component Analysis (PCA) projection using pca_init:\n\nY_pca = pca_init(X, 2; scale=1.0f0)\n\ncolors = Dict(\"Iris-setosa\" => \"#636EFA\", \"Iris-versicolor\" => \"#EF553B\", \"Iris-virginica\" => \"#00CC96\")\ntraces_pca = Config[]\n\nfor species in unique_species\n    idx = findall(==(species), labels)\n    push!(traces_pca, Config(\n        x = Y_pca[1, idx],\n        y = Y_pca[2, idx],\n        mode = \"markers\",\n        name = species,\n        text = [\"Species: $(species)<br>Sepal L: $(round(X[1, i], digits=2))<br>Petal L: $(round(X[3, i], digits=2))\" for i in idx],\n        hoverinfo = \"text\",\n        marker = Config(size=9, color=colors[species], opacity=0.85)\n    ))\nend\n\nlayout_pca = Config(\n    title = \"Iris Dataset: Linear 2D PCA Projection\",\n    xaxis = Config(title = \"PC 1\", zeroline=false),\n    yaxis = Config(title = \"PC 2\", zeroline=false),\n    hovermode = \"closest\"\n)\n\nPlot(traces_pca, layout_pca)\n\nWhile linear PCA separates Setosa, Versicolor and Virginica remain heavily overlapping along the linear subspace.\n\n","title":"2. Baseline: 2D PCA Projection"},{"category":"section","location":"tutorial_iris/#3.-Computing-the-Nearest-Neighbor-Graph","page":"Iris Dataset","text":"In Trimap.jl, neighbor search is decoupled from optimization. We use SimilaritySearch.ExhaustiveSearch with Euclidean distance Dist.L2():\n\ndb = MatrixDatabase(X)\nindex = ExhaustiveSearch(Dist.L2(), db)\nctx = GenericContext()\n\nk = 20\nknns, dists = allknn(index, ctx, k)\n\nprintln(\"knns matrix: \", size(knns), \" of \", eltype(knns))\nprintln(\"dists matrix: \", size(dists), \" of \", eltype(dists))\n\n","title":"3. Computing the Nearest Neighbor Graph"},{"category":"section","location":"tutorial_iris/#4.-Fitting-Non-Parametric-TriMAP","page":"Iris Dataset","text":"We fit TriMAP using X, knns, and dists (PCA initialization is performed automatically from X):\n\nmodel = fit(\n    Trimap,\n    X,\n    knns,\n    dists;\n    maxoutdim=2,\n    n_epochs=400,\n    learning_rate=0.1\n)\n\nY = model.embedding # (2, 150)\nprintln(\"Embedding size: \", size(Y))\n\n","title":"4. Fitting Non-Parametric TriMAP"},{"category":"section","location":"tutorial_iris/#5.-TriMAP-2D-Embedding-Visualization-with-PlotlyLight","page":"Iris Dataset","text":"traces = Config[]\n\nfor species in unique_species\n    idx = findall(==(species), labels)\n    push!(traces, Config(\n        x = Y[1, idx],\n        y = Y[2, idx],\n        mode = \"markers\",\n        name = species,\n        text = [\"Species: $(species)<br>Sepal L: $(round(X[1, i], digits=2))<br>Petal L: $(round(X[3, i], digits=2))\" for i in idx],\n        hoverinfo = \"text\",\n        marker = Config(size=9, color=colors[species], opacity=0.85)\n    ))\nend\n\nlayout = Config(\n    title = \"TriMAP 2D Embedding of Fisher's Iris\",\n    xaxis = Config(title = \"TriMAP 1\", zeroline=false),\n    yaxis = Config(title = \"TriMAP 2\", zeroline=false),\n    hovermode = \"closest\"\n)\n\nPlot(traces, layout)\n\nThe resulting 2D projection demonstrates clear, well-separated cluster boundaries for Setosa, while preserving the continuous transition and relative density between Versicolor and Virginica.","title":"5. TriMAP 2D Embedding Visualization with PlotlyLight"},{"category":"section","location":"tutorial_moons_spirals/#Tutorial-2:-Non-linear-Manifolds-Two-Moons-and-Spirals","page":"Two Moons and Spirals","text":"In this tutorial, we evaluate TriMAP on synthetic non-linear manifolds: the classic Two Moons dataset and Intertwined Spirals. For each dataset, we:\n\nVisualize the original 2D input manifold.\nCompute and visualize the linear 2D PCA baseline.\nCompute the nearest neighbor graph using SimilaritySearch.SearchGraph or ExhaustiveSearch.\nApply TriMAP (with representative fft negative sampling and sample_probs weighting) and visualize the unwrapped manifold with PlotlyLight.jl.\n\n","title":"Tutorial 2: Non-linear Manifolds - Two Moons and Spirals"},{"category":"section","location":"tutorial_moons_spirals/#1.-Two-Moons-Manifold","page":"Two Moons and Spirals","text":"","title":"1. Two Moons Manifold"},{"category":"section","location":"tutorial_moons_spirals/#1.1-Generating-and-Visualizing-2D-Input-Data","page":"Two Moons and Spirals","text":"using SimilaritySearch, Trimap\nusing PlotlyLight, Random, Statistics\n\nRandom.seed!(42)\n\nfunction make_moons(n_samples=1200, noise=0.05f0)\n    n_half = div(n_samples, 2)\n    theta1 = range(0f0, Float32(π), length=n_half)\n    theta2 = range(0f0, Float32(π), length=n_half)\n    \n    x1 = cos.(theta1) .+ noise .* randn(Float32, n_half)\n    y1 = sin.(theta1) .+ noise .* randn(Float32, n_half)\n    \n    x2 = 1.0f0 .- cos.(theta2) .+ noise .* randn(Float32, n_half)\n    y2 = 1.0f0 .- sin.(theta2) .- 0.5f0 .+ noise .* randn(Float32, n_half)\n    \n    X = Matrix{Float32}(hcat(vcat(x1', y1'), vcat(x2', y2')))\n    labels = vcat(fill(\"Moon A\", n_half), fill(\"Moon B\", n_half))\n    X, labels\nend\n\nX_moons, labels_moons = make_moons(1200, 0.05f0)\nprintln(\"Moons dataset size: \", size(X_moons))\n\n# 1. Visualize Original 2D Input Data\ntraces_input_moons = [\n    Config(\n        x = X_moons[1, labels_moons .== \"Moon A\"],\n        y = X_moons[2, labels_moons .== \"Moon A\"],\n        mode = \"markers\",\n        name = \"Moon A\",\n        marker = Config(size=4, color=\"#1f77b4\", opacity=0.85)\n    ),\n    Config(\n        x = X_moons[1, labels_moons .== \"Moon B\"],\n        y = X_moons[2, labels_moons .== \"Moon B\"],\n        mode = \"markers\",\n        name = \"Moon B\",\n        marker = Config(size=4, color=\"#ff7f0e\", opacity=0.85)\n    )\n]\n\nPlot(traces_input_moons, Config(title=\"Two Moons: Original 2D Input Space\"))","title":"1.1 Generating and Visualizing 2D Input Data"},{"category":"section","location":"tutorial_moons_spirals/#1.2-Baseline:-2D-PCA-Projection","page":"Two Moons and Spirals","text":"Y_pca_moons = pca_init(X_moons, 2; scale=1.0f0)\n\ntraces_pca_moons = [\n    Config(\n        x = Y_pca_moons[1, labels_moons .== \"Moon A\"],\n        y = Y_pca_moons[2, labels_moons .== \"Moon A\"],\n        mode = \"markers\",\n        name = \"Moon A\",\n        marker = Config(size=4, color=\"#1f77b4\", opacity=0.85)\n    ),\n    Config(\n        x = Y_pca_moons[1, labels_moons .== \"Moon B\"],\n        y = Y_pca_moons[2, labels_moons .== \"Moon B\"],\n        mode = \"markers\",\n        name = \"Moon B\",\n        marker = Config(size=4, color=\"#ff7f0e\", opacity=0.85)\n    )\n]\n\nPlot(traces_pca_moons, Config(title=\"Two Moons: Linear 2D PCA Projection\"))","title":"1.2 Baseline: 2D PCA Projection"},{"category":"section","location":"tutorial_moons_spirals/#1.3-Approximate-k-NN-with-SearchGraph","page":"Two Moons and Spirals","text":"Instead of exhaustive search, we construct an approximate SearchGraph and optimize its search parameters for a target recall (90%):\n\ndb_moons = MatrixDatabase(X_moons)\nG = SearchGraph(Dist.L2(), db_moons)\nctx_g = SearchGraphContext()\nindex!(G, ctx_g)\n\n# Optimize for 90% recall\noptimize_index!(G, ctx_g, MinRecall(0.90))\n\nk = 20\nknns_moons, dists_moons = allknn(G, ctx_g, k)","title":"1.3 Approximate k-NN with SearchGraph"},{"category":"section","location":"tutorial_moons_spirals/#1.4-TriMAP-Embedding","page":"Two Moons and Spirals","text":"model_moons = fit(\n    Trimap,\n    X_moons,\n    knns_moons,\n    dists_moons;\n    maxoutdim=2,\n    n_epochs=400,\n    learning_rate=0.1\n)\n\nY_moons = model_moons.embedding\n\ntraces_trimap_moons = [\n    Config(\n        x = Y_moons[1, labels_moons .== \"Moon A\"],\n        y = Y_moons[2, labels_moons .== \"Moon A\"],\n        mode = \"markers\",\n        name = \"Moon A\",\n        marker = Config(size=4, color=\"#1f77b4\", opacity=0.85)\n    ),\n    Config(\n        x = Y_moons[1, labels_moons .== \"Moon B\"],\n        y = Y_moons[2, labels_moons .== \"Moon B\"],\n        mode = \"markers\",\n        name = \"Moon B\",\n        marker = Config(size=4, color=\"#ff7f0e\", opacity=0.85)\n    )\n]\n\nPlot(traces_trimap_moons, Config(title=\"Two Moons: TriMAP 2D Projection (SearchGraph k-NN)\"))\n\n","title":"1.4 TriMAP Embedding"},{"category":"section","location":"tutorial_moons_spirals/#2.-3-Intertwined-Spirals-Manifold","page":"Two Moons and Spirals","text":"Complex spiral structures are notoriously challenging for dimensionality reduction methods because standard random negative sampling can mistakenly pull distant spiral arms together across gaps.\n\nBelow we first fit TriMAP with the default uniform random negative sampling (§2.4), then compare it against negative samples drawn from Farthest First Traversal (fft) landmarks and weighted by their Voronoi basin sizes (sample_probs from res.nn, §2.5). fft/sample_probs sampling is most useful on large datasets, where it concentrates negative samples on a small, diverse landmark set instead of drawing uniformly from all n points — it does not, by itself, fix the local inlier/outlier margin pressure that causes the artifacts you'll see below on this particular tightly-wound toy manifold.","title":"2. 3 Intertwined Spirals Manifold"},{"category":"section","location":"tutorial_moons_spirals/#2.1-Generating-and-Visualizing-2D-Spiral-Data","page":"Two Moons and Spirals","text":"using SimilaritySearch, Trimap\nusing PlotlyLight, Random, Statistics\n\nRandom.seed!(42)\n\nfunction make_spirals(n_points_per_arm=600, noise=0.03f0)\n    arms = []\n    labels = []\n    \n    for arm in 1:3\n        theta = range(0.5f0, Float32(4.0 * π), length=n_points_per_arm) .+ Float32((arm - 1) * (2π / 3))\n        r = range(0.2f0, 2.0f0, length=n_points_per_arm)\n        \n        x = r .* cos.(theta) .+ noise .* randn(Float32, n_points_per_arm)\n        y = r .* sin.(theta) .+ noise .* randn(Float32, n_points_per_arm)\n        \n        push!(arms, vcat(x', y'))\n        push!(labels, fill(\"Spiral $(arm)\", n_points_per_arm))\n    end\n    \n    X = Matrix{Float32}(hcat(arms...))\n    labels_all = vcat(labels...)\n    X, labels_all\nend\n\nX_spirals, labels_spirals = make_spirals(600, 0.03f0)\nprintln(\"Spirals dataset size: \", size(X_spirals))\n\ncolors = Dict(\"Spiral 1\" => \"#636EFA\", \"Spiral 2\" => \"#EF553B\", \"Spiral 3\" => \"#00CC96\")\n\n# 1. Visualize Original 2D Input Data\ntraces_input_spirals = Config[]\nfor arm_name in [\"Spiral 1\", \"Spiral 2\", \"Spiral 3\"]\n    idx = findall(==(arm_name), labels_spirals)\n    push!(traces_input_spirals, Config(\n        x = X_spirals[1, idx],\n        y = X_spirals[2, idx],\n        mode = \"markers\",\n        name = arm_name,\n        marker = Config(size=3, color=colors[arm_name], opacity=0.85)\n    ))\nend\n\nPlot(traces_input_spirals, Config(title=\"3 Intertwined Spirals: Original 2D Input Space\"))","title":"2.1 Generating and Visualizing 2D Spiral Data"},{"category":"section","location":"tutorial_moons_spirals/#2.2-Baseline:-2D-PCA-Projection","page":"Two Moons and Spirals","text":"Y_pca_spirals = pca_init(X_spirals, 2; scale=1.0f0)\n\ntraces_pca_spirals = Config[]\nfor arm_name in [\"Spiral 1\", \"Spiral 2\", \"Spiral 3\"]\n    idx = findall(==(arm_name), labels_spirals)\n    push!(traces_pca_spirals, Config(\n        x = Y_pca_spirals[1, idx],\n        y = Y_pca_spirals[2, idx],\n        mode = \"markers\",\n        name = arm_name,\n        marker = Config(size=3, color=colors[arm_name], opacity=0.85)\n    ))\nend\n\nPlot(traces_pca_spirals, Config(title=\"3 Spirals: Linear 2D PCA Projection\"))\n\nBecause PCA is a linear projection, it preserves the rotation and entanglement of the spirals without unwrapping the non-linear manifold.","title":"2.2 Baseline: 2D PCA Projection"},{"category":"section","location":"tutorial_moons_spirals/#2.3-Computing-k-NN","page":"Two Moons and Spirals","text":"db_spirals = MatrixDatabase(X_spirals)\nindex_spirals = ExhaustiveSearch(Dist.L2(), db_spirals)\nctx = GenericContext()\nknns_spirals, dists_spirals = allknn(index_spirals, ctx, 20)","title":"2.3 Computing k-NN"},{"category":"section","location":"tutorial_moons_spirals/#2.4-TriMAP-2D-Embedding","page":"Two Moons and Spirals","text":"model_spirals = fit(\n    Trimap,\n    X_spirals,\n    knns_spirals,\n    dists_spirals;\n    maxoutdim=2,\n    weight_adj=0.15,\n    n_epochs=400,\n    learning_rate=0.1\n)\n\nY_spirals = model_spirals.embedding\n\ntraces_trimap_spirals = Config[]\nfor arm_name in [\"Spiral 1\", \"Spiral 2\", \"Spiral 3\"]\n    idx = findall(==(arm_name), labels_spirals)\n    push!(traces_trimap_spirals, Config(\n        x = Y_spirals[1, idx],\n        y = Y_spirals[2, idx],\n        mode = \"markers\",\n        name = arm_name,\n        marker = Config(size=4, color=colors[arm_name], opacity=0.85)\n    ))\nend\n\nlayout_spirals = Config(\n    title = \"3 Spirals: TriMAP 2D Embedding (uniform random negatives)\",\n    xaxis = Config(title = \"TriMAP 1\", zeroline=false),\n    yaxis = Config(title = \"TriMAP 2\", zeroline=false),\n    hovermode = \"closest\"\n)\n\nPlot(traces_trimap_spirals, layout_spirals)","title":"2.4 TriMAP 2D Embedding"},{"category":"section","location":"tutorial_moons_spirals/#2.5-TriMAP-with-fft/sample_probs-Negative-Sampling","page":"Two Moons and Spirals","text":"Instead of drawing negative samples uniformly from all n points, we pick a diverse landmark set with Farthest First Traversal (fft) and weight each landmark by how many points fall into its Voronoi cell (res_fft.nn counts). This concentrates the \"global\" (n_random) triplets on a small representative sample instead of the full dataset — useful when n is large and uniform sampling becomes wasteful or noisy.\n\nres_fft = fft(Dist.L2(), db_spirals, 60)\nsample_centers = res_fft.centers\n\ncounts = Dict{UInt32, Float32}()\nfor c in res_fft.nn\n    counts[c] = get(counts, c, 0f0) + 1f0\nend\nsample_probs = Float32[get(counts, c, 0f0) / length(res_fft.nn) for c in sample_centers]\n\nmodel_spirals_fft = fit(\n    Trimap,\n    X_spirals,\n    knns_spirals,\n    dists_spirals;\n    sample=sample_centers,\n    sample_probs=sample_probs,\n    maxoutdim=2,\n    weight_adj=0.15,\n    n_epochs=400,\n    learning_rate=0.1\n)\n\nY_spirals_fft = model_spirals_fft.embedding\n\ntraces_trimap_spirals_fft = Config[]\nfor arm_name in [\"Spiral 1\", \"Spiral 2\", \"Spiral 3\"]\n    idx = findall(==(arm_name), labels_spirals)\n    push!(traces_trimap_spirals_fft, Config(\n        x = Y_spirals_fft[1, idx],\n        y = Y_spirals_fft[2, idx],\n        mode = \"markers\",\n        name = arm_name,\n        marker = Config(size=4, color=colors[arm_name], opacity=0.85)\n    ))\nend\n\nlayout_spirals_fft = Config(\n    title = \"3 Spirals: TriMAP 2D Embedding (fft landmark negatives)\",\n    xaxis = Config(title = \"TriMAP 1\", zeroline=false),\n    yaxis = Config(title = \"TriMAP 2\", zeroline=false),\n    hovermode = \"closest\"\n)\n\nPlot(traces_trimap_spirals_fft, layout_spirals_fft)\n\nOn this specific dataset the two runs look similar: the three arms are a single continuous 1D curve each, with no real cluster boundary between \"near\" and \"far\" neighbors along the same arm, so the inlier/outlier margin mechanism (designed for clustered data) fights the smoothness of the curve regardless of how negative samples are drawn. fft/sample_probs sampling pays off most on datasets with genuine cluster structure and large n, such as the FashionMNIST tutorial.","title":"2.5 TriMAP with fft/sample_probs Negative Sampling"},{"category":"section","location":"tutorial_prime_factors/#Tutorial-3:-Non-Vector-Metric-Spaces-Prime-Factorization","page":"Prime Factorization","text":"One of the most powerful aspects of combining SimilaritySearch.jl with Trimap.jl is that TriMAP can embed any metric space, not just Euclidean vector spaces.\n\nIn this tutorial (inspired by the SimilaritySearch.jl prime factors tutorial), we represent integers by their prime factor sets, measure their arithmetic similarity using the Jaccard distance, and project the resulting discrete metric space into a continuous 2D manifold. We also demonstrate Parametric TriMAP using Lux.jl to project unseen integers.\n\n","title":"Tutorial 3: Non-Vector Metric Spaces - Prime Factorization"},{"category":"section","location":"tutorial_prime_factors/#1.-Prime-Factorization-as-a-Metric-Space","page":"Prime Factorization","text":"Every integer n ge 2 has a unique prime factorization. For example:\n\n12 = 2^2 times 3 implies textfactors(12) = 2 3\n18 = 2 times 3^2 implies textfactors(18) = 2 3\n35 = 5 times 7 implies textfactors(35) = 5 7\n\nTwo integers are similar if they share a high proportion of prime factors. We quantify this using the Jaccard distance: d(A B) = 1 - fracA cap BA cup B\n\nusing SimilaritySearch, Trimap\nusing Primes, PlotlyLight, Lux, Random, Statistics\n\nRandom.seed!(42)\n\n# Consider integers from 2 to 3000\nN_max = 3000\nnumbers = collect(2:N_max)\nn_items = length(numbers)\n\n# Extract unique prime factors for each number\nprime_sets = [unique(factor(Vector, num)) for num in numbers]\nprintln(\"First 5 numbers and their prime factors:\")\nfor i in 1:5\n    println(\"  $(numbers[i]) => $(prime_sets[i])\")\nend\n\n# Build a binary prime-indicator feature matrix (dim = number of primes <= N_max)\nall_primes = primes(N_max)\nprime_to_idx = Dict(p => i for (i, p) in enumerate(all_primes))\nn_features = length(all_primes)\n\nfunction featurize(factor_set)\n    v = zeros(Float32, n_features)\n    for p in factor_set\n        if haskey(prime_to_idx, p)\n            v[prime_to_idx[p]] = 1.0f0\n        end\n    end\n    v\nend\n\nX_primes = hcat([featurize(fs) for fs in prime_sets]...) # (n_features, n_items)\nprintln(\"Prime indicator matrix: \", size(X_primes))\n\n","title":"1. Prime Factorization as a Metric Space"},{"category":"section","location":"tutorial_prime_factors/#2.-Indexing-with-SearchGraph-and-Dist.Sets.Jaccard","page":"Prime Factorization","text":"We wrap the factor sets in a VectorDatabase and index them using SimilaritySearch.SearchGraph with Dist.Sets.Jaccard():\n\ndb_primes = VectorDatabase(prime_sets)\nG = SearchGraph(Dist.Sets.Jaccard(), db_primes)\nctx = SearchGraphContext()\nindex!(G, ctx)\n\n# Optimize for fast 90% recall search\noptimize_index!(G, ctx, MinRecall(0.90))\n\nk = 20\nknns_primes, dists_primes = allknn(G, ctx, k)\n\n","title":"2. Indexing with SearchGraph and Dist.Sets.Jaccard"},{"category":"section","location":"tutorial_prime_factors/#3.-FFT-Landmark-Selection-and-Probability-Weighting","page":"Prime Factorization","text":"We apply Farthest First Traversal (fft) to select 100 well-distributed landmark numbers across the prime factorization metric space, and weight each landmark by the size of its Voronoi cluster in res_fft.nn:\n\nres_fft = fft(Dist.Sets.Jaccard(), db_primes, 100)\nsample_centers = res_fft.centers\n\n# Compute relative frequencies/probabilities\ncounts = Dict{UInt32, Float32}()\nfor c in res_fft.nn\n    counts[c] = get(counts, c, 0f0) + 1f0\nend\nsample_probs = Float32[get(counts, c, 0f0) / length(res_fft.nn) for c in sample_centers]\n\nprintln(\"Total landmarks: \", length(sample_centers))\n\n","title":"3. FFT Landmark Selection and Probability Weighting"},{"category":"section","location":"tutorial_prime_factors/#4.-Fitting-Non-Parametric-TriMAP","page":"Prime Factorization","text":"model_primes = fit(\n    Trimap,\n    X_primes,\n    knns_primes,\n    dists_primes;\n    maxoutdim=2,\n    n_epochs=400,\n    learning_rate=0.1\n)\n\nY_primes = model_primes.embedding\n\n","title":"4. Fitting Non-Parametric TriMAP"},{"category":"section","location":"tutorial_prime_factors/#5.-Interactive-Visualization-with-PlotlyLight","page":"Prime Factorization","text":"We categorize numbers by their dominant arithmetic characteristics (powers of 2, multiples of 3, 5, 7, and prime numbers) and display interactive tooltips showing the exact factorization when hovering over any point:\n\n# Categorize for visualization\nfunction categorize(num, factors)\n    if isprime(num)\n        return \"Prime Numbers\"\n    elseif 2 in factors && length(factors) == 1\n        return \"Powers of 2\"\n    elseif 2 in factors\n        return \"Even Numbers (multiples of 2)\"\n    elseif 3 in factors\n        return \"Multiples of 3\"\n    elseif 5 in factors\n        return \"Multiples of 5\"\n    else\n        return \"Other Composites\"\n    end\nend\n\ncategories = [categorize(numbers[i], prime_sets[i]) for i in 1:n_items]\nunique_cats = [\"Prime Numbers\", \"Powers of 2\", \"Even Numbers (multiples of 2)\", \"Multiples of 3\", \"Multiples of 5\", \"Other Composites\"]\npalette = Dict(\n    \"Prime Numbers\" => \"#EF553B\",\n    \"Powers of 2\" => \"#636EFA\",\n    \"Even Numbers (multiples of 2)\" => \"#AB63FA\",\n    \"Multiples of 3\" => \"#00CC96\",\n    \"Multiples of 5\" => \"#FFA15A\",\n    \"Other Composites\" => \"#7F7F7F\"\n)\n\ntraces_primes = Config[]\nfor cat in unique_cats\n    idx = findall(==(cat), categories)\n    push!(traces_primes, Config(\n        x = Y_primes[1, idx],\n        y = Y_primes[2, idx],\n        mode = \"markers\",\n        name = cat,\n        text = [\"n = $(numbers[i])<br>Factors: $(prime_sets[i])\" for i in idx],\n        hoverinfo = \"text\",\n        marker = Config(size=5, color=palette[cat], opacity=0.85)\n    ))\nend\n\nlayout_primes = Config(\n    title = \"TriMAP 2D Embedding of Integers under Jaccard Prime Metric\",\n    xaxis = Config(title = \"TriMAP 1\", zeroline=false),\n    yaxis = Config(title = \"TriMAP 2\", zeroline=false),\n    hovermode = \"closest\"\n)\n\nPlot(traces_primes, layout_primes)\n\n","title":"5. Interactive Visualization with PlotlyLight"},{"category":"section","location":"tutorial_prime_factors/#6.-Parametric-TriMAP:-Out-of-Sample-Prediction","page":"Prime Factorization","text":"What if we want to project new unseen integers (e.g. n in 3001 dots 3150) without re-optimizing the entire dataset?\n\nWe use ParametricTrimap backed by a Lux neural network trained on the multi-hot feature vectors X_primes:\n\n# Fit Parametric TriMAP\npmodel = fit(\n    ParametricTrimap,\n    X_primes,\n    knns_primes,\n    dists_primes;\n    maxoutdim=2,\n    hidden_dims=(128, 64),\n    n_epochs=400,\n    learning_rate=0.1\n)\n\n# Unseen test integers\ntest_numbers = collect(3001:3150)\ntest_prime_sets = [unique(factor(Vector, num)) for num in test_numbers]\nX_test = hcat([featurize(fs) for fs in test_prime_sets]...)\n\n# Instant O(1) projection\nY_test = predict(pmodel, X_test)\nprintln(\"Projected $(size(Y_test, 2)) unseen test integers into 2D coordinates!\")\n\nThe parametric model successfully maps new unseen numbers into the appropriate arithmetic geometric regions of the manifold in constant time O(1).","title":"6. Parametric TriMAP: Out-of-Sample Prediction"},{"category":"section","location":"#Trimap.jl","page":"Home","text":"Fast and flexible TriMAP dimensionality reduction in Julia, powered by SimilaritySearch.jl and Lux.jl.\n\n","title":"Trimap.jl"},{"category":"section","location":"#Overview","page":"Home","text":"TriMAP (Large-scale Dimensionality Reduction Using Triplets) is a state-of-the-art manifold learning and dimensionality reduction technique. It preserves both local neighborhood structure (inliers vs margin outliers) and global topological structure (inliers vs random negative samples) through triplet constraints.\n\nTrimap.jl provides:\n\nDecoupled Metric Search & Optimization: Receives precomputed k-NN matrices (knns::AbstractMatrix{UInt32} and dists::AbstractMatrix{Float32}) from any search index in SimilaritySearch.jl, allowing exact search (ExhaustiveSearch) or high-throughput approximate search (SearchGraph).\nSupport for Arbitrary Metric Spaces: Embed non-vector data (graphs, strings, sets, histograms, audio signatures) as easily as Euclidean vectors.\nRepresentative Negative Sampling: Landmark selection using Farthest First Traversal (fft) and weighted cluster probabilities (sample_probs computed from .nn).\nParametric TriMAP: Deep neural network embedding model powered by Lux.jl, enabling O(1) out-of-sample projection of unseen queries.\nPure Julia & GPU/Differentiable: Powered by Optimisers.jl and Zygote.jl.\n\n","title":"Overview"},{"category":"section","location":"#Installation","page":"Home","text":"Trimap.jl can be installed using the Julia package manager:\n\nusing Pkg\nPkg.add(\"Trimap\")\n\nOr from GitHub:\n\nPkg.add(url=\"https://github.com/sadit/Trimap.jl.git\")\n\n","title":"Installation"},{"category":"section","location":"#Quick-Start","page":"Home","text":"using SimilaritySearch, Trimap\n\n# 1. High-dimensional data\nX = randn(Float32, 10, 1000) # 10 dimensions, 1000 points\ndb = MatrixDatabase(X)\n\n# 2. Compute k-nearest neighbors\nindex = ExhaustiveSearch(Dist.L2(), db)\nctx = GenericContext()\nk = 25 # inliers + outliers + margin\nknns, dists = allknn(index, ctx, k)\n\n# 3. Fit non-parametric TriMAP (PCA initialization is default)\nmodel = fit(Trimap, X, knns, dists; maxoutdim=2)\n\n# model.embedding is a (2, 1000) Float32 matrix\n\n","title":"Quick Start"},{"category":"section","location":"#Related-Packages","page":"Home","text":"SimSearchManifoldLearning.jl: Companion package in the SimilaritySearch.jl ecosystem. It implements UMAP natively for similarity search graphs and serves as a bridge to seamlessly integrate SimilaritySearch.jl indices with the diverse manifold learning algorithms in ManifoldLearning.jl (such as Isomap, LLE, Laplacian Eigenmaps, and Hessian LLE).\nUMAP.jl: A Julia implementation of the Uniform Manifold Approximation and Projection (UMAP) algorithm for dimension reduction.\nManifoldLearning.jl: A Julia package for non-linear dimensionality reduction algorithms.\nSimilaritySearch.jl: High-performance similarity search and approximate nearest neighbor index structures in Julia.\n\n","title":"Related Packages"},{"category":"section","location":"#API-Reference","page":"Home","text":"","title":"API Reference"},{"category":"type","location":"#Trimap.ParametricTrimap","page":"Home","text":"ParametricTrimap\n\nParametric TriMAP model backed by a neural network model (Lux). Allows O(1) projection of out-of-sample points via predict(m, X_new).\n\n\n\n\n\n","title":"Trimap.ParametricTrimap"},{"category":"type","location":"#Trimap.Trimap","page":"Home","text":"Trimap\n\nNon-parametric TriMAP embedding model. Holds the learned embedding coordinates embedding.\n\n\n\n\n\n","title":"Trimap.Trimap"},{"category":"method","location":"#Trimap.fit-Tuple{Type{ParametricTrimap}, AbstractMatrix{Float32}, AbstractMatrix{UInt32}, AbstractMatrix{Float32}}","page":"Home","text":"fit(::Type{ParametricTrimap}, X::AbstractMatrix{Float32}, knns::AbstractMatrix{UInt32}, dists::AbstractMatrix{Float32};\n    model=nothing,\n    maxoutdim=2,\n    n_epochs=400,\n    hidden_dims=(128, 64),\n    sample=nothing,\n    sample_probs=nothing,\n    n_inliers=nothing,\n    n_outliers=nothing,\n    n_random=nothing,\n    weight_adj=0.1,\n    opt=nothing,\n    learning_rate=0.1,\n    rng=Random.default_rng(),\n    verbose=false) -> ParametricTrimap\n\nFits a Parametric TriMAP model using a neural network (Lux), training on high-dimensional features X and triplet constraints generated from nearest neighbor IDs (knns::AbstractMatrix{UInt32}) and distances (dists::AbstractMatrix{Float32}).\n\nArguments\n\nX: (dim × n) input data matrix of type Float32.\nknns: (k × n) matrix of nearest neighbor IDs of type UInt32.\ndists: (k × n) matrix of nearest neighbor distances of type Float32.\n\nKeyword Arguments\n\nmodel: Custom Lux layer/network mapping dim -> maxoutdim. If nothing, a multilayer perceptron with hidden_dims and ReLU activations is constructed.\nmaxoutdim: Target embedding dimension (default: 2).\nn_epochs: Optimization epochs (default: 400).\nhidden_dims: Tuple of hidden layer dimensions for default MLP (default: (128, 64)).\nsample: Optional subset of sample points / landmarks from which negative samples are drawn (e.g. Vector{UInt32} from fft(dist, db, k).centers). If nothing (default), negative samples are uniformly drawn from 1:n.\nsample_probs: Optional vector of sampling probabilities or weights corresponding to each element in sample (or 1:n). If nothing (default), uniform sampling is used.\nn_inliers: Number of inlier neighbors. Default: round(Int, 3k/4).\nn_outliers: Number of outlier neighbors. Default: round(Int, k/4).\nn_random: Number of random negative samples. Default: equal to n_outliers.\nweight_adj: Weight adjustment parameter. Default: 0.1.\nopt: Optimizer instance from Optimisers.jl (default: AdamW(learning_rate)).\nlearning_rate: Learning rate if opt is not specified. Default: 0.1.\nrng: Random number generator. Default: Random.default_rng().\nverbose: Whether to log iteration loss. Default: false.\n\nExamples\n\nExact search with ExhaustiveSearch and fft negative sampling with cluster probabilities\n\nusing SimilaritySearch, Trimap\n\n# Create dataset and exact search index\nX = randn(Float32, 10, 500)\ndb = MatrixDatabase(X)\nindex = ExhaustiveSearch(Dist.L2(), db)\n\n# Compute exact all-kNN\nk = 25\nctx = GenericContext()\nknns, dists = allknn(index, ctx, k)\n\n# Extract a diverse sample for negative sampling using Farthest First Traversal (fft)\nres_fft = fft(Dist.L2(), db, 50)\nsample_centers = res_fft.centers\n\n# Compute relative frequencies/probabilities from res_fft.nn\ncounts = Dict{UInt32, Float32}()\nfor c in res_fft.nn\n    counts[c] = get(counts, c, 0f0) + 1f0\nend\nsample_probs = Float32[get(counts, c, 0f0) / length(res_fft.nn) for c in sample_centers]\n\n# Fit Parametric TriMAP with weighted fft negative sampling\npmodel = fit(ParametricTrimap, X, knns, dists; sample=sample_centers, sample_probs=sample_probs, maxoutdim=2, hidden_dims=(64, 32))\n\n# Predict embeddings for unseen points\nX_new = randn(Float32, 10, 50)\nY_new = predict(pmodel, X_new)\n\nApproximate search with SearchGraph (recall < 1)\n\nusing SimilaritySearch, Trimap\n\n# Create dataset and search graph index\nX = randn(Float32, 10, 500)\ndb = MatrixDatabase(X)\nG = SearchGraph(Dist.L2(), db)\nctx = SearchGraphContext()\nindex!(G, ctx)\n\n# Optimize index for fast approximate search with target recall < 1.0\noptimize_index!(G, ctx, MinRecall(0.85))\n\n# Compute approximate all-kNN\nk = 25\nknns, dists = allknn(G, ctx, k)\n\n# Fit Parametric TriMAP\npmodel = fit(ParametricTrimap, X, knns, dists; maxoutdim=2)\n\n\n\n\n\n","title":"Trimap.fit"},{"category":"method","location":"#Trimap.fit-Tuple{Type{Trimap.Trimap}, AbstractMatrix{Float32}, AbstractMatrix{UInt32}, AbstractMatrix{Float32}}","page":"Home","text":"fit(::Type{Trimap}, X::AbstractMatrix{Float32}, knns::AbstractMatrix{UInt32}, dists::AbstractMatrix{Float32};\n    maxoutdim=2,\n    n_epochs=400,\n    Y_init=nothing,\n    sample=nothing,\n    sample_probs=nothing,\n    n_inliers=nothing,\n    n_outliers=nothing,\n    n_random=nothing,\n    weight_adj=0.1,\n    opt=nothing,\n    learning_rate=0.1,\n    rng=Random.default_rng(),\n    verbose=false) -> Trimap\n\nFits non-parametric TriMAP dimensionality reduction given high-dimensional features X, precomputed nearest neighbor IDs (knns::AbstractMatrix{UInt32}), and distances (dists::AbstractMatrix{Float32}).\n\nArguments\n\nX: (dim × n) input data matrix of type Float32.\nknns: (k × n) matrix of nearest neighbor IDs of type UInt32 (e.g. from allknn or searchbatch).\ndists: (k × n) matrix of nearest neighbor distances of type Float32.\n\nKeyword Arguments\n\nmaxoutdim: Target embedding dimension (default: 2).\nn_epochs: Optimization iterations / epochs (default: 400).\nY_init: Initial embedding matrix of size (maxoutdim, n). If nothing (default), initialized via pca_init(X, maxoutdim). Pass :random for Gaussian noise.\nsample: Optional subset of sample points / landmarks from which negative samples are drawn (e.g. Vector{UInt32} from fft(dist, db, k).centers). If nothing (default), negative samples are drawn from 1:n.\nsample_probs: Optional vector of sampling probabilities or weights corresponding to each element in sample (or 1:n). If nothing (default), uniform sampling is used.\nn_inliers: Number of nearest neighbors treated as inliers (local structure). Default: round(Int, 3k/4).\nn_outliers: Number of further neighbors treated as margin outliers. Default: round(Int, k/4).\nn_random: Number of random negative samples per inlier (global structure). Default: equal to n_outliers.\nweight_adj: Weight adjustment parameter controlling the influence of distance gaps. Default: 0.1.\nopt: Optimizer instance from Optimisers.jl (default: AdamW(learning_rate)).\nlearning_rate: Learning rate if opt is not specified. Default: 0.1.\nrng: Random number generator. Default: Random.default_rng().\nverbose: Whether to log iteration loss. Default: false.\n\nExamples\n\nExact search with ExhaustiveSearch and fft negative sampling with cluster probabilities\n\nusing SimilaritySearch, Trimap\n\n# Create dataset and exact search index\nX = randn(Float32, 10, 500)\ndb = MatrixDatabase(X)\nindex = ExhaustiveSearch(Dist.L2(), db)\n\n# Compute exact all-kNN (e.g., k = 25)\nk = 25\nctx = GenericContext()\nknns, dists = allknn(index, ctx, k)\n\n# Extract a diverse sample for negative sampling using Farthest First Traversal (fft)\nres_fft = fft(Dist.L2(), db, 50)\nsample_centers = res_fft.centers\n\n# Compute relative frequencies/probabilities from res_fft.nn\ncounts = Dict{UInt32, Float32}()\nfor c in res_fft.nn\n    counts[c] = get(counts, c, 0f0) + 1f0\nend\nsample_probs = Float32[get(counts, c, 0f0) / length(res_fft.nn) for c in sample_centers]\n\n# Fit non-parametric TriMAP with weighted fft negative sampling (PCA initialization is automatic)\nmodel = fit(Trimap, X, knns, dists; sample=sample_centers, sample_probs=sample_probs, maxoutdim=2)\n# model.embedding is of size (2, 500)\n\nApproximate search with SearchGraph (recall < 1)\n\nusing SimilaritySearch, Trimap\n\n# Create dataset and search graph index\nX = randn(Float32, 10, 500)\ndb = MatrixDatabase(X)\nG = SearchGraph(Dist.L2(), db)\nctx = SearchGraphContext()\nindex!(G, ctx)\n\n# Optimize index for fast approximate search with target recall < 1.0\noptimize_index!(G, ctx, MinRecall(0.85))\n\n# Compute approximate all-kNN\nk = 25\nknns, dists = allknn(G, ctx, k)\n\n# Fit non-parametric TriMAP\nmodel = fit(Trimap, X, knns, dists; maxoutdim=2)\n\n\n\n\n\n","title":"Trimap.fit"},{"category":"method","location":"#Trimap.generate_triplets-Tuple{AbstractMatrix{UInt32}, AbstractMatrix{Float32}}","page":"Home","text":"generate_triplets(knns::AbstractMatrix{UInt32}, dists::AbstractMatrix{Float32};\n                  sample=nothing,\n                  sample_probs=nothing,\n                  n_inliers=nothing,\n                  n_outliers=nothing,\n                  n_random=nothing,\n                  weight_adj=0.1,\n                  rng=Random.default_rng()) -> (triplets_i, triplets_j, triplets_k, weights)\n\nGenerates TriMAP triplet constraints (i, j, k) where item j is closer to i than item k in the original space.\n\nArguments\n\nknns: (k × n) matrix of nearest neighbor IDs of type UInt32 (e.g., from allknn or searchbatch).\ndists: (k × n) matrix of corresponding distances of type Float32.\n\nKeyword Arguments\n\nsample: Optional subset of sample points / landmarks from which negative samples are drawn (e.g., Vector{UInt32} from fft(dist, db, k).centers or AbstractMatrix{Float32}). If nothing (default), negative samples are drawn from 1:n.\nsample_probs: Optional vector of sampling probabilities or weights corresponding to each element in sample (or 1:n). If nothing (default), uniform sampling is used.\nn_inliers: Number of nearest neighbors treated as inliers (local structure). Default: round(Int, 3k/4).\nn_outliers: Number of further neighbors treated as margin outliers. Default: round(Int, k/4).\nn_random: Number of random negative samples per inlier (global structure). Default: equal to n_outliers.\nweight_adj: Weight adjustment parameter controlling the influence of distance gaps. Default: 0.1.\nrng: Random number generator for negative sampling. Default: Random.default_rng().\n\nReturns\n\n(triplets_i, triplets_j, triplets_k, weights): 1-based integer indices (Int32) and float weights (Float32) for triplet loss.\n\nExamples\n\nExact search with ExhaustiveSearch and fft negative sampling with relative cluster probabilities\n\nusing SimilaritySearch, Trimap\n\n# Create dataset and exact search index\nX = randn(Float32, 10, 500)\ndb = MatrixDatabase(X)\nindex = ExhaustiveSearch(Dist.L2(), db)\n\n# Compute exact all-kNN (e.g., k = 25)\nk = 25\nctx = GenericContext()\nknns, dists = allknn(index, ctx, k)\n\n# Extract diverse landmarks using Farthest First Traversal (fft)\nres_fft = fft(Dist.L2(), db, 50)\nsample_centers = res_fft.centers\n\n# Count how many points choose each center in res_fft.nn to compute relative probabilities\ncounts = Dict{UInt32, Float32}()\nfor c in res_fft.nn\n    counts[c] = get(counts, c, 0f0) + 1f0\nend\nsample_probs = Float32[get(counts, c, 0f0) / length(res_fft.nn) for c in sample_centers]\n\n# Generate triplets using the weighted fft sample\ntriplets_i, triplets_j, triplets_k, weights = generate_triplets(\n    knns, dists;\n    sample=sample_centers,\n    sample_probs=sample_probs,\n    n_inliers=10,\n    n_outliers=5,\n    n_random=5\n)\n\nApproximate search with SearchGraph (recall < 1)\n\nusing SimilaritySearch, Trimap\n\n# Create dataset and search graph index\nX = randn(Float32, 10, 500)\ndb = MatrixDatabase(X)\nG = SearchGraph(Dist.L2(), db)\nctx = SearchGraphContext()\nindex!(G, ctx)\n\n# Optimize index for fast approximate search with target recall < 1.0\noptimize_index!(G, ctx, MinRecall(0.85))\n\n# Compute approximate all-kNN\nk = 25\nknns, dists = allknn(G, ctx, k)\n\n# Generate triplets\ntriplets_i, triplets_j, triplets_k, weights = generate_triplets(knns, dists; n_inliers=10, n_outliers=5, n_random=5)\n\n\n\n\n\n","title":"Trimap.generate_triplets"},{"category":"method","location":"#Trimap.optimize_embedding_triplets-Tuple{AbstractMatrix{Float32}, AbstractVector{<:Integer}, AbstractVector{<:Integer}, AbstractVector{<:Integer}, AbstractVector{Float32}}","page":"Home","text":"optimize_embedding_triplets(Y_init::AbstractMatrix{Float32}, triplets_i, triplets_j, triplets_k, weights;\n                            max_iters=400, opt=Optimisers.AdamW(0.1f0), verbose=false)\n\nOptimizes the low-dimensional embedding Y given precomputed triplets and weights.\n\n\n\n\n\n","title":"Trimap.optimize_embedding_triplets"},{"category":"method","location":"#Trimap.pca_init-Tuple{AbstractMatrix{Float32}, Integer}","page":"Home","text":"pca_init(X::AbstractMatrix{Float32}, out_dim::Integer; scale::Real=0.01f0) -> Matrix{Float32}\n\nComputes PCA-based initialization of size (out_dim, n) from high-dimensional data X of size (dim, n), scaled by scale.\n\n\n\n\n\n","title":"Trimap.pca_init"},{"category":"method","location":"#Trimap.predict-Tuple{ParametricTrimap, AbstractMatrix{Float32}}","page":"Home","text":"predict(m::ParametricTrimap, X::AbstractMatrix{Float32}) -> Matrix{Float32}\n\nMaps unseen high-dimensional data points to embedding coordinates using the trained neural network.\n\n\n\n\n\n","title":"Trimap.predict"},{"category":"method","location":"#Trimap.train_parametric_trimap-Tuple{AbstractLuxLayer, AbstractMatrix{Float32}, AbstractVector{<:Integer}, AbstractVector{<:Integer}, AbstractVector{<:Integer}, AbstractVector{Float32}}","page":"Home","text":"train_parametric_trimap(model::LuxCore.AbstractLuxLayer, X::AbstractMatrix{Float32},\n                        triplets_i, triplets_j, triplets_k, weights;\n                        max_iters=200, opt=Optimisers.AdamW(0.001f0),\n                        rng=Random.default_rng(), verbose=false)\n\nTrains a parametric neural network model (Lux) to map high-dimensional data X to low-dimensional embedding.\n\n\n\n\n\n","title":"Trimap.train_parametric_trimap"},{"category":"method","location":"#Trimap.trimap_loss-Tuple{AbstractMatrix, AbstractVector{<:Integer}, AbstractVector{<:Integer}, AbstractVector{<:Integer}, AbstractVector{<:Real}}","page":"Home","text":"trimap_loss(Y::AbstractMatrix,\n            triplets_i::AbstractVector{<:Integer},\n            triplets_j::AbstractVector{<:Integer},\n            triplets_k::AbstractVector{<:Integer},\n            weights::AbstractVector{<:Real})\n\nComputes the TriMAP triplet loss over embedding Y (where columns represent data points). Uses the Student-t kernel:\n\nd_ij = 1 + y_i - y_j^2\nd_ik = 1 + y_i - y_k^2\nmathcalL = sum_(ijk) w_ijk fracd_ijd_ij + d_ik\n\n\n\n\n\n","title":"Trimap.trimap_loss"},{"category":"section","location":"tutorial_fashion_mnist/#Tutorial-4:-Real-World-Images-FashionMNIST","page":"FashionMNIST","text":"In this tutorial we apply Trimap.jl to the classic FashionMNIST dataset: 28×28 grayscale images of clothing items across 10 categories. We load a subset via MLDatasets.jl, reduce the raw pixel space with PCA, build an approximate nearest-neighbor graph with SimilaritySearch.SearchGraph, and embed with TriMAP using fft/sample_probs landmark negative sampling — a setting that pays off much more here than on the small synthetic spirals, since FashionMNIST has genuine cluster structure and a much larger n.\n\n","title":"Tutorial 4: Real-World Images - FashionMNIST"},{"category":"section","location":"tutorial_fashion_mnist/#1.-Loading-a-FashionMNIST-Subset","page":"FashionMNIST","text":"The full test split has 10,000 images; we take a balanced subset (200 per class = 2,000 images) so the tutorial runs quickly.\n\nENV[\"DATADEPS_ALWAYS_ACCEPT\"] = \"true\"  # allow the dataset download without an interactive prompt\n\nusing SimilaritySearch, Trimap\nusing MLDatasets, PlotlyLight, Random, Statistics\n\nRandom.seed!(42)\n\nfmnist = FashionMNIST(:test)\nclass_names = fmnist.metadata[\"class_names\"]\n\nn_per_class = 200\nidxs = Int[]\nfor c in 0:9\n    class_idxs = findall(==(c), fmnist.targets)\n    append!(idxs, class_idxs[randperm(length(class_idxs))[1:n_per_class]])\nend\nshuffle!(idxs)\n\nimages = fmnist.features[:, :, idxs]           # (28, 28, n)\nlabels = fmnist.targets[idxs]                  # 0-based class ids\nn = length(idxs)\n\nX = Float32.(reshape(images, 28 * 28, n))       # (784, n) flattened pixel vectors\nprintln(\"FashionMNIST subset size: \", size(X), \"  classes: \", class_names)\n\n","title":"1. Loading a FashionMNIST Subset"},{"category":"section","location":"tutorial_fashion_mnist/#2.-Baseline:-2D-PCA-Projection","page":"FashionMNIST","text":"Y_pca = pca_init(X, 2; scale=1.0f0)\n\ncolors = [\"#636EFA\",\"#EF553B\",\"#00CC96\",\"#AB63FA\",\"#FFA15A\",\"#19D3F3\",\"#FF6692\",\"#B6E880\",\"#FF97FF\",\"#FECB52\"]\ntraces_pca = Config[]\nfor c in 0:9\n    idx = findall(==(c), labels)\n    push!(traces_pca, Config(\n        x = Y_pca[1, idx],\n        y = Y_pca[2, idx],\n        mode = \"markers\",\n        name = class_names[c + 1],\n        marker = Config(size=4, color=colors[c + 1], opacity=0.8)\n    ))\nend\n\nPlot(traces_pca, Config(title=\"FashionMNIST: Linear 2D PCA Projection\"))\n\nLinear PCA on raw pixels gives some coarse grouping (e.g. footwear vs. clothing), but categories such as Shirt, Pullover, Coat and T-shirt/top overlap heavily.\n\n","title":"2. Baseline: 2D PCA Projection"},{"category":"section","location":"tutorial_fashion_mnist/#3.-Preprocessing-and-Approximate-k-NN","page":"FashionMNIST","text":"784-dimensional raw pixels make exact nearest-neighbor search unnecessarily expensive. We first project to 50 PCA dimensions (a standard preprocessing step before manifold learning), then build an approximate SearchGraph index:\n\nX_pca50 = pca_init(X, 50; scale=1.0f0)\n\ndb = MatrixDatabase(X_pca50)\nG = SearchGraph(Dist.L2(), db)\nctx = SearchGraphContext()\nindex!(G, ctx)\n\noptimize_index!(G, ctx, MinRecall(0.9))\n\nk = 25\nknns, dists = allknn(G, ctx, k)\n\n","title":"3. Preprocessing and Approximate k-NN"},{"category":"section","location":"tutorial_fashion_mnist/#4.-TriMAP-Embedding-with-fft/sample_probs-Negative-Sampling","page":"FashionMNIST","text":"With 2,000 points, drawing negative samples uniformly from all of them is wasteful and noisier than necessary. Instead we pick a diverse landmark set with Farthest First Traversal (fft) and weight each landmark by its Voronoi basin size:\n\nres_fft = fft(Dist.L2(), db, 120)\nsample_centers = res_fft.centers\n\ncounts = Dict{UInt32, Float32}()\nfor c in res_fft.nn\n    counts[c] = get(counts, c, 0f0) + 1f0\nend\nsample_probs = Float32[get(counts, c, 0f0) / length(res_fft.nn) for c in sample_centers]\n\nmodel = fit(\n    Trimap,\n    X_pca50,\n    knns,\n    dists;\n    sample=sample_centers,\n    sample_probs=sample_probs,\n    maxoutdim=2,\n    n_epochs=400,\n    learning_rate=0.1\n)\n\nY = model.embedding\nprintln(\"Embedding size: \", size(Y))\n\n","title":"4. TriMAP Embedding with fft/sample_probs Negative Sampling"},{"category":"section","location":"tutorial_fashion_mnist/#5.-TriMAP-2D-Embedding-Visualization","page":"FashionMNIST","text":"traces = Config[]\nfor c in 0:9\n    idx = findall(==(c), labels)\n    push!(traces, Config(\n        x = Y[1, idx],\n        y = Y[2, idx],\n        mode = \"markers\",\n        name = class_names[c + 1],\n        marker = Config(size=4, color=colors[c + 1], opacity=0.8)\n    ))\nend\n\nlayout = Config(\n    title = \"FashionMNIST: TriMAP 2D Embedding (784 → 50 PCA → 2D)\",\n    xaxis = Config(title = \"TriMAP 1\", zeroline=false),\n    yaxis = Config(title = \"TriMAP 2\", zeroline=false),\n    hovermode = \"closest\"\n)\n\nPlot(traces, layout)\n\nFootwear (Sandal, Sneaker, Ankle boot), Trouser and Bag typically form tight, well-separated groups, since they look quite different from one another in raw pixel space. The upper-body garments (T-shirt/top, Pullover, Coat, Shirt, Dress) remain partially intermixed — that overlap is a genuine property of raw-pixel similarity between visually similar garments, not an artifact of the embedding; a learned feature space (e.g. via ParametricTrimap) or more epochs can sharpen it further.","title":"5. TriMAP 2D Embedding Visualization"}]
}
